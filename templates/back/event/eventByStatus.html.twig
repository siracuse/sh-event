{% extends 'baseAdmin.html.twig' %}


{% block body %}
{#    <h2>{{title}}</h2>#}


    <div class="card">
        <div class="card-header"></div>
        <div class="card-body">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Titre</th>
                    <th>Date</th>
                    <th>Organisateur</th>
                    <th>Statut</th>
                    <th class="action-menu">Actions</th>
                </tr>
                </thead>

                <tbody>
{#                {% for event in events %}#}

{#                    <tr>#}
{#                        <td>{{ event.title }}</td>#}
{#                        <td>{{ event.date | date('d.m.Y') }}</td>#}
{#                        <td>{{ event.organiser.name }}</td>#}
{#                        <td>{{ event.status }}</td>#}
{#                        <td class="action">#}
{#                            <a href="{{ path('admin.event.view', {id:event.id}) }}">#}
{#                                <i class="fa-solid fa-circle-info"></i>#}
{#                            </a>#}

{#                        </td>#}
{#                    </tr>#}

{#                {% endfor %}#}
                </tbody>

            </table>
        </div>
    </div>





{% endblock %}